#include "main.h"
/**
 * _isdigit - checks for an integer
 * @c: Interger to be checked
 * Return: 1 for an interger or 0 if otherwise
 */
int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}
return (0);
}

